# Orchestration-VM runtime deps (CPU). Mirrors pyproject.toml dependencies block.
# Pin a real lockfile (uv.lock or pip-compile output) on first env build.

keras>=3.7,<4.0
jax>=0.4.30,<0.5
jaxlib>=0.4.30,<0.5
numpy>=2.0,<3.0
tokenizers>=0.20,<0.22
datasets>=3.0,<4.0
transformers>=4.46,<5.0
optax>=0.2,<0.3
orbax-checkpoint>=0.6,<0.8
wandb>=0.18,<0.20
runpod>=1.7,<2.0
pyyaml>=6.0,<7.0
pydantic>=2.8,<3.0
tqdm>=4.66,<5.0
rich>=13.7,<14.0
boto3>=1.35,<2.0
structlog>=24.4,<26.0
tenacity>=9.0,<10.0
xxhash>=3.5,<4.0
pyarrow>=17.0,<19.0
# Compression libs that some HF datasets' loader scripts import directly.
# Without these, runtime fails with `ModuleNotFoundError: No module named 'zstandard'`
# when streaming e.g. EleutherAI/proof-pile-2.
zstandard>=0.22,<1.0
