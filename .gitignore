# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.eggs/
build/
dist/

# Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# Local artifacts (push to object storage instead)
# Anchored to root only — sub-`artifacts/` folders (e.g. pilots/*/artifacts/)
# are intentionally tracked as small metadata snapshots.
/artifacts/
*.safetensors
*.gguf
*.ckpt
checkpoints/
wandb/
mlruns/

# Secrets
.env
.env.*
!.env.example
*.pem
credentials.json

# OS
.DS_Store
Thumbs.db

# Nested fresh-lineage program repo (own .git — NEVER tracked by this legacy repo)
sama-7b/
